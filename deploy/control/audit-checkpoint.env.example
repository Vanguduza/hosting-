# Install as root-owned mode 0600 at /etc/dial-hosting/audit-checkpoint.env.
DB_HOST=
DB_NAME=hosting
DB_USER=
DB_PASSWORD_FILE=/etc/dial-hosting/audit-db-password
# Add provider-specific Restic environment variables for the remote repository.
