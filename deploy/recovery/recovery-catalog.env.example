# Copy to /etc/dial-hosting/recovery-catalog.env as root, mode 0600.
# Configure the repository-specific credential variables needed by the
# selected remote Restic backends (for example AWS_ACCESS_KEY_ID and
# AWS_SECRET_ACCESS_KEY). Do not put backup encryption passwords here;
# point the JSON configuration at independently protected password files.
