[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "dial-hosting-release-client"
version = "0.1.0"
description = "Application-scoped release client for DIAL Hosting"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
include = ["dial_hosting*"]
